[build-system]
requires = ["setuptools>=75.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "webserp"
version = "0.2.0"
description = "Local-agent web CLI — search, fetch Markdown, and map links"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{ name = "PaperBoardOfficial" }]
keywords = ["search", "metasearch", "cli", "scraping", "serp"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Utilities",
]
dependencies = [
    "curl_cffi>=0.7.0",
    "lxml>=5.0.0",
]

[project.scripts]
webcli-lite = "webserp.webcli_lite:main"
webserp = "webserp.cli:main"
webfetch = "webserp.webfetch_cli:main"

[project.urls]
Homepage = "https://github.com/PaperBoardOfficial/webserp"

[tool.setuptools.packages.find]
where = ["src"]
